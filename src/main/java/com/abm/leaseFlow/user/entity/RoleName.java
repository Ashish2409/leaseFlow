package com.abm.leaseFlow.user.entity;

/**
 * Canonical role names. The "ROLE_" prefix is required by Spring Security.
 */
public enum RoleName {
    ROLE_PLATFORM_ADMIN,
    ROLE_PROPERTY_MANAGER,
    ROLE_LEASING_AGENT,
    ROLE_RESIDENT
}
